using UnityEngine;
using System.Collections;
using UnityEngine.EventSystems;

public class overThisObject : MonoBehaviour {

    public GameObject inventory;

    public void Over()
    {
        //inventory.GetComponent<Inventory>().currentlyOver(gameObject);
    }
}